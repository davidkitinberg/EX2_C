
#include <stdio.h>
#include <string.h>
#ifndef matrix
#define matrix


void readMatrix(int n, int mat[n][n]);  // Function to read the matrix
void printMatrix(int n, int mat[n][n]); // Function to print the matrix
void transposeMatrix(int n, int mat[n][n]);  // Function to transpose the matrix

#endif